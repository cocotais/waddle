Blockly.JavaScript['html_noa_tag_a'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<a>\n${inner}\n</a>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_abbr'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<abbr>\n${inner}\n</abbr>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_address'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<address>\n${inner}\n</address>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_area'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<area>\n${inner}\n</area>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_article'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<article>\n${inner}\n</article>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_aside'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<aside>\n${inner}\n</aside>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_audio'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<audio>\n${inner}\n</audio>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_b'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<b>\n${inner}\n</b>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_base'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<base>\n${inner}\n</base>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_bdi'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<bdi>\n${inner}\n</bdi>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_bdo'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<bdo>\n${inner}\n</bdo>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_blockquote'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<blockquote>\n${inner}\n</blockquote>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_body'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<body>\n${inner}\n</body>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_br'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<br>\n${inner}\n</br>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_button'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<button>\n${inner}\n</button>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_canvas'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<canvas>\n${inner}\n</canvas>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_caption'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<caption>\n${inner}\n</caption>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_cite'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<cite>\n${inner}\n</cite>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_code'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<code>\n${inner}\n</code>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_col'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<col>\n${inner}\n</col>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_colgroup'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<colgroup>\n${inner}\n</colgroup>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_command'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<command>\n${inner}\n</command>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_data'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<data>\n${inner}\n</data>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_datalist'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<datalist>\n${inner}\n</datalist>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_dd'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<dd>\n${inner}\n</dd>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_del'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<del>\n${inner}\n</del>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_details'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<details>\n${inner}\n</details>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_div'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<div>\n${inner}\n</div>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_dfn'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<dfn>\n${inner}\n</dfn>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_dialog'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<dialog>\n${inner}\n</dialog>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_dl'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<dl>\n${inner}\n</dl>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_dt'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<dt>\n${inner}\n</dt>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_em'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<em>\n${inner}\n</em>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_embed'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<embed>\n${inner}\n</embed>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_fieldset'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<fieldset>\n${inner}\n</fieldset>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_figcaption'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<figcaption>\n${inner}\n</figcaption>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_figure'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<figure>\n${inner}\n</figure>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_footer'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<footer>\n${inner}\n</footer>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_form'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<form>\n${inner}\n</form>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_h1'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<h1>\n${inner}\n</h1>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_head'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<head>\n${inner}\n</head>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_header'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<header>\n${inner}\n</header>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_hr'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<hr>\n${inner}\n</hr>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_html'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<html>\n${inner}\n</html>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_i'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<i>\n${inner}\n</i>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_iframe'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<iframe>\n${inner}\n</iframe>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_img'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<img>\n${inner}\n</img>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_input'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<input>\n${inner}\n</input>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_ins'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<ins>\n${inner}\n</ins>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_isindex'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<isindex>\n${inner}\n</isindex>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_kbd'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<kbd>\n${inner}\n</kbd>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_keygen'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<keygen>\n${inner}\n</keygen>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_label'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<label>\n${inner}\n</label>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_legend'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<legend>\n${inner}\n</legend>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_li'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<li>\n${inner}\n</li>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_link'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<link>\n${inner}\n</link>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_main'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<main>\n${inner}\n</main>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_map'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<map>\n${inner}\n</map>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_mark'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<mark>\n${inner}\n</mark>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_menu'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<menu>\n${inner}\n</menu>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_menuitem'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<menuitem>\n${inner}\n</menuitem>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_meta'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<meta>\n${inner}\n</meta>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_meter'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<meter>\n${inner}\n</meter>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_nav'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<nav>\n${inner}\n</nav>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_noscript'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<noscript>\n${inner}\n</noscript>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_object'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<object>\n${inner}\n</object>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_ol'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<ol>\n${inner}\n</ol>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_optgroup'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<optgroup>\n${inner}\n</optgroup>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_option'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<option>\n${inner}\n</option>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_output'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<output>\n${inner}\n</output>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_p'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<p>\n${inner}\n</p>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_param'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<param>\n${inner}\n</param>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_pre'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<pre>\n${inner}\n</pre>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_progress'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<progress>\n${inner}\n</progress>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_q'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<q>\n${inner}\n</q>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_rp'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<rp>\n${inner}\n</rp>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_rt'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<rt>\n${inner}\n</rt>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_ruby'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<ruby>\n${inner}\n</ruby>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_s'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<s>\n${inner}\n</s>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_samp'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<samp>\n${inner}\n</samp>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_script'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<script>\n${inner}\n</script>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_section'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<section>\n${inner}\n</section>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_select'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<select>\n${inner}\n</select>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_small'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<small>\n${inner}\n</small>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_source'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<source>\n${inner}\n</source>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_span'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<span>\n${inner}\n</span>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_strong'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<strong>\n${inner}\n</strong>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_style'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<style>\n${inner}\n</style>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_sub'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<sub>\n${inner}\n</sub>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_summary'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<summary>\n${inner}\n</summary>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_sup'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<sup>\n${inner}\n</sup>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_svg'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<svg>\n${inner}\n</svg>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_table'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<table>\n${inner}\n</table>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_tbody'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<tbody>\n${inner}\n</tbody>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_td'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<td>\n${inner}\n</td>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_template'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<template>\n${inner}\n</template>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_textarea'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<textarea>\n${inner}\n</textarea>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_tfoot'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<tfoot>\n${inner}\n</tfoot>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_th'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<th>\n${inner}\n</th>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_thead'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<thead>\n${inner}\n</thead>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_time'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<time>\n${inner}\n</time>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_title'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<title>\n${inner}\n</title>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_tr'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<tr>\n${inner}\n</tr>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_track'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<track>\n${inner}\n</track>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_u'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<u>\n${inner}\n</u>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_ul'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<ul>\n${inner}\n</ul>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_var'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<var>\n${inner}\n</var>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_video'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<video>\n${inner}\n</video>`;
    return code;
};

Blockly.JavaScript['html_noa_tag_wbr'] = function(block){
    var inner = Blockly.JavaScript.statementToCode(block, "INNER")
    let code = `<wbr>\n${inner}\n</wbr>`;
    return code;
};

